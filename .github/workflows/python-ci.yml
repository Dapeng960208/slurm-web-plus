---
name: Backend Tests
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  python_tests:
    name: Python unit tests (Python 3.12)
    runs-on: ubuntu-latest
    env:
      RESULTS_DIR: ci-results/backend-python-3.12
      ARTIFACT_NAME: backend-python-3.12
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run backend unit test command
        run: |
          COMMAND="sudo apt-get update && sudo apt-get install -y build-essential python3-dev libldap2-dev libsasl2-dev libcairo2-dev libgirepository-1.0-dev libgirepository-2.0-dev libpango1.0-dev && python -m pip install --upgrade pip && pip install . '.[agent]' '.[gateway]' '.[tests]' && pytest --junitxml=${RESULTS_DIR}/junit.xml"
          node .github/scripts/run-ci-command.mjs \
            --results-dir "$RESULTS_DIR" \
            --artifact-name "$ARTIFACT_NAME" \
            --label "backend-unit-tests" \
            --junit-path "${RESULTS_DIR}/junit.xml" \
            --command "$COMMAND"

      - name: Ensure backend unit test result
        if: always()
        run: |
          node .github/scripts/ensure-ci-result.mjs \
            --results-dir "$RESULTS_DIR" \
            --artifact-name "$ARTIFACT_NAME" \
            --label "backend-unit-tests" \
            --command "pytest --junitxml=${RESULTS_DIR}/junit.xml"

      - name: Upload backend unit test artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.RESULTS_DIR }}
          if-no-files-found: error
